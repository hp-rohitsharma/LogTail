package com.sample.logtail.reader;
import java.io.File;
import java.util.LinkedList;

public class Main {

	public static void main(String[] args) {
		
		/*WriterThread writerThread = new WriterThread(new File("log.txt"));
		
		LinkedList<String> queue= new LinkedList<String>();
		ReaderThread readerThread = new ReaderThread(new File("log.txt"), queue);
		
		PrinterThread printerThread = new PrinterThread(queue,null);
		
		new Thread(writerThread).start();
		new Thread(readerThread).start();
		new Thread(printerThread).start();*/
	}
}
